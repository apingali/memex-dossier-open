kvlayer:
  app_name: dossier
  namespace: models
  storage_type: redis
  storage_addresses: ['localhost:6379']

dossier.store:
  feature_indexes: ['NAME', 'keywords']

logging:
  root:
    level: DEBUG
